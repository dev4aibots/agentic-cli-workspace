# Security Boundaries

- **Shell Execution**: Inherits user privileges. Highly dangerous without isolation.
- **Secrets**: API keys must not be logged in trace outputs.
- **Iteration limits**: Hard budget on maximum turns prevents runaway costs.